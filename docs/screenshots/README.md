# Screenshots

Save the screenshots referenced by the project root `README.md` here, then
uncomment the image lines in its **## Screenshots** section.

Suggested captures (PNG, ~1280px wide):

| File             | What to show                                              |
| ---------------- | -------------------------------------------------------- |
| `dashboard.png`  | Dashboard with the statistic cards and charts            |
| `companies.png`  | Companies table — sorting / search / pagination visible  |
| `dark-mode.png`  | Any page with the dark theme enabled                     |

Tip: log in with `admin` / `admin123`, add or remove a record, and capture the
dashboard updating to make the demo feel alive.
